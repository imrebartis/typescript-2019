const drink = {
  color: 'brown',
  carbonated: 'true',
  sugar: 40
}

// type alias
type Drink = [string, boolean, number];

const cico: Drink = ['brown', true, 40];
const frucola: Drink = ['grey', true, 70];
